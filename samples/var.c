#include <stdio.h>

int main(void) {

    // 整数型
    int x;
    x = 1;

    // 実数型
    float y = 1.0;

    // 文字型（1文字）
    char c = 'a';

    printf("x = %d, y = %f, c = %c\n", x, y, c);

    return 0;
}
